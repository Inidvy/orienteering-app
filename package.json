{
  "name": "orienteering-app",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test -w @orienteering/verification-core && npm run test -w @orienteering/run-engine",
    "typecheck": "npm run typecheck -w @orienteering/verification-core && npm run typecheck -w @orienteering/run-engine"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
