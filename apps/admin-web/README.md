# admin-web

Desktop-first admin (design doc: "Flag registry, lifecycle & web admin"):
flag CRUD with photo upload + map placement, course authoring (pick flags,
drag to order), missing-flag report inbox, tag provisioning/replacement
checklist. Same Supabase backend, admin role via RLS.

Scaffold only — features are task T7 in docs/design.md.
